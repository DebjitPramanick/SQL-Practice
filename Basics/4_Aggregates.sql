# 1. Count names from table
select count(FirstName) as count_name from persons;

# 2. Sum all ages from table
select sum(age) as totalage from persons;

# 2. Average all ages from table
select avg(age) as totalage from persons;